<%= form_for(@rake_task) do |f| %>
  <% if @rake_task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rake_task.errors.count, "error") %> prohibited this rake_task from being saved:</h2>

      <ul>
      <% @rake_task.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :task %><br />
    <%= f.text_field :task %>
  </div>
  <div class="field">
    <%= f.label :active %><br />
    <%= f.check_box :active %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
